#include <stdio.h>
#include <string.h>

int main(){
	int testcase, flag, len;
	char word[105];
	 scanf("%d", &testcase);
	 
	 for(int loop = 1; loop <= testcase; loop++){
	 	scanf("%s", word);
	 	len = strlen(word);
	 	
	 	for(int x = 0; x < len; x++){
	 		
		}
	 	
	 }
	
	return 0;
}
